<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

  <ul>
    <% results.forEach(function(r) { %>
        <li>
            <strong><%= r.Id %></strong>
            <small><%= r.FirstName %></small>
            <small><%= r.LastName %></small>
        </li>
    <% }); %>
</ul>
    
</body>
</html>